name: Build and Push Docker Images

on:
  push:
    branches: [master, main]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [master, main]

env:
  REGISTRY_GHCR: ghcr.io
  IMAGE_PREFIX: nesohq/bgce-archive

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      archive-admin: ${{ steps.filter.outputs.archive-admin }}
      archive-client: ${{ steps.filter.outputs.archive-client }}
      cortex: ${{ steps.filter.outputs.cortex }}
      postal: ${{ steps.filter.outputs.postal }}
    steps:
      - uses: actions/checkout@v4
      
      - name: Detect changed services
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            archive-admin:
              - 'archive-admin/**'
            archive-client:
              - 'archive-client/**'
            cortex:
              - 'cortex/**'
            postal:
              - 'postal/**'

  type-check:
    needs: detect-changes
    if: needs.detect-changes.outputs.archive-admin == 'true'
    uses: ./.github/workflows/type-check.yml
    with:
      service: archive-admin

  build-and-push:
    needs: detect-changes
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        service: [archive-admin, archive-client, cortex, postal]
    uses: ./.github/workflows/docker-build.yml
    with:
      service: ${{ matrix.service }}
      should-build: ${{ needs.detect-changes.outputs[matrix.service] == 'true' || github.ref_type == 'tag' }}
    secrets: inherit

  # ============================================================================
  # DEPLOYMENT: Uncomment when infrastructure repository is ready
  # ============================================================================
  # update-deployment:
  #   needs: [detect-changes, build-and-push]
  #   if: github.event_name != 'pull_request' && github.ref == 'refs/heads/master'
  #   strategy:
  #     matrix:
  #       service: [archive-admin, archive-client, cortex, postal]
  #   uses: ./.github/workflows/update-k8s.yml
  #   with:
  #     service: ${{ matrix.service }}
  #     should-update: ${{ needs.detect-changes.outputs[matrix.service] == 'true' }}
  #   secrets: inherit
  # ============================================================================

  # Temporary deployment status message (remove when uncommenting above)
  update-deployment:
    needs: [detect-changes, build-and-push]
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [archive-admin, archive-client, cortex, postal]
    
    steps:
      - name: Deployment Status
        if: needs.detect-changes.outputs[matrix.service] == 'true'
        run: |
          echo "## 🚀 Deployment Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Service:** ${{ matrix.service }}" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ⏳ Pending Infrastructure Setup" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📋 **Next Steps:**" >> $GITHUB_STEP_SUMMARY
          echo "- Infrastructure repository setup is in progress" >> $GITHUB_STEP_SUMMARY
          echo "- Kubernetes deployment automation will be enabled once infra repo is ready" >> $GITHUB_STEP_SUMMARY
          echo "- Manual deployment may be required at this time" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 **Image Ready:**" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "ghcr.io/nesohq/bgce-archive/${{ matrix.service }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💡 Once the infrastructure repository is ready, automatic K8s deployment will be enabled." >> $GITHUB_STEP_SUMMARY
