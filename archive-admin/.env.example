# API Configuration
# For local development (yarn dev): use full URLs to backend services
# For Docker/production: use relative paths (/api/v1) - nginx will proxy
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_POSTAL_API_BASE_URL=http://localhost:8081/api/v1

# App Configuration
VITE_APP_NAME=Archive Admin
VITE_APP_VERSION=1.0.0

# Production Docker deployment:
# Set VITE_API_BASE_URL=/api/v1 and VITE_POSTAL_API_BASE_URL=/api/v1
# Then configure nginx proxy via CORTEX_API_URL env var at runtime
